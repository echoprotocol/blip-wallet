/* eslint-disable */

import { Dispatch as ReduxDispatch, Store as ReduxStore } from 'redux';

export const counterStateType = {
  counter: 0
};

export const Action = {
  type: ''
};

export const GetState = () => counterStateType;

export const Dispatch = ReduxDispatch;

export const Store = ReduxStore;
